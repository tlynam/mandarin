<%= form_for(phrase) do |f| %>
  <% if phrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phrase.errors.count, "error") %> prohibited this phrase from being saved:</h2>

      <ul>
      <% phrase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :english %>
    <%= f.text_area :english %>
  </div>

  <div class="field">
    <%= f.label :pinyin %>
    <%= f.text_area :pinyin %>
  </div>

  <div class="field">
    <%= f.label :simplified %>
    <%= f.text_area :simplified %>
  </div>

  <div class="field">
    <%= f.label :spell_13_to_confirm_not_spam %>
    <%= f.text_field :spam %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
